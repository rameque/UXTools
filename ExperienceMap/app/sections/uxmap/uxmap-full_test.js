'use strict';

describe('uxMap.full module', function() {

    beforeEach(module('uxMap.full'));

    describe('uxmap Full Controller', function() {

        it('should ....', inject(function($controller) {
            //spec body
            var uxMapFullCtrl = $controller('uxMapFull');
            expect(uxMapFullCtrl).toBeDefined();
        }));

    });
});
